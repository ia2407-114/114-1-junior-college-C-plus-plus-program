
//題目2.承襲第6週的題目2，寫一個主程式，從鍵盤輸入本金(p) 年利率(r)，每年年底定期定額存入金額，以及存款目標(aim)
//，再寫一個副程式，計算需要幾年才能達成目標，並傳回年數(int型態)

#include <stdio.h>
int like(double p, double r, int money, double aims);
int main()
{

    int i = 0, money;
    double p, r, aims;

    printf("你的本金? ");
    scanf_s("%lf", &p);
    printf("你的利率? ");
    scanf_s("%lf", &r);
    printf_s("你每年底定期存入的金額? ");
    scanf_s("%d", &money);
    printf("你的存款目標是多少? ");
    scanf_s("%lf", &aims);


    printf("第 %d 年才能達成目標\n", like(p, r, money, aims));

    return 0;
}



int like(double p, double r, int money, double aims)
{
    int i = 0;
    while (p < aims) {  // 本金未達目標時持續計算，不符合停止
        p = p * (1 + r) + money;
        i = i + 1;
    }

    return i;
}
