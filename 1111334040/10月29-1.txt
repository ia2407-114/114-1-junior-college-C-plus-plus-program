#include <stdio.h>

float BMI(float height, float weight);

int main()
{
	float height, weight;
	printf("請輸入身高、體重，\n");
	scanf_s("%f %f", &height, &weight);
	printf("你的BMI指數為%.1f", BMI(height, weight));
}
float BMI(float height, float weight)
{
	return weight / (height * height);
}
