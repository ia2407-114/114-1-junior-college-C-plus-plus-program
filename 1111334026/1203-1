#include <stdio.h>
double fact(int n);
int main(void)
{
	int n;
	printf("輸入n:");
	scanf_s("%d", &n);
	double e = 0.0;
	for (int k=0; k <= n; k++)
	{
		e += + 1.0 / fact(k);
	}
	printf("e=%lf\n", e);
	return 0;
}

double fact(int n)
{
	if (n <= 0)
		return 1;
	else
		return n * fact(n - 1);

}
