#include <iostream>
using namespace std;
#include"shape.h"


int main() {
    Shape* s;
    s = new Ball(5);
    cout << "圓錐體體積:" << s->Volume() << endl;
    s = new Cone(4, 6);
    cout << "球體體積:" << s->Volume() << endl;
    s = new Cylinder(4, 6);
    cout << "圓柱體體積:" << s->Volume() << endl;


    return 0;
}
