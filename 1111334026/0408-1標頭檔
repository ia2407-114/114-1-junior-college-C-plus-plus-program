// Fig. 9.6: fig09_06.cpp 
// Constructor with default arguments.
#include <iostream>
#include <stdexcept>
#include "Date.h" // include definition of class Date from Date.h
using namespace std;

int main()
{
        int y, m, d;

        cout << "請輸入日期 : ";
        cin >> y >> m >> d;

        try {
            Date userDate(y, m, d);
            cout << "\n西元日期: ";
            userDate.printUniversal();

            cout << "\n民國日期: ";
            userDate.printStandard();
            cout << endl;
        }
        catch (invalid_argument& e) {
            cerr << "\n錯誤: " << e.what() << endl;

            cerr << "\n\nException while initializing t5: " << e.what() << endl;
        }
            return 0;
        
    /*Date t1; // all arguments defaulted
    Date t2(2); // year specified; month and day defaulted
    Date t3(21, 2); // year and month specified; day defaulted 
    Date t4(12, 2, 12); // year, month and day specified

    cout << "Constructed with:\n\nt1: all arguments defaulted\n  ";
    t1.printUniversal(); // 00:00:00
    cout << "\n  ";
    t1.printStandard(); // 12:00:00 AM

    cout << "\n\nt2: year specified; month and day defaulted\n  ";
    t2.printUniversal(); // 02:00:00
    cout << "\n  ";
    t2.printStandard(); // 2:00:00 AM

    cout << "\n\nt3: year and month specified; day defaulted\n  ";
    t3.printUniversal(); // 21:34:00
    cout << "\n  ";
    t3.printStandard(); // 9:34:00 PM

    cout << "\n\nt4: year, month and day specified\n  ";
    t4.printUniversal(); // 12:25:42
    cout << "\n  ";
    t4.printStandard(); // 12:25:42 PM

    // attempt to initialize t6 with invalid values
    try
    {
        Date t5(27, 74, 99); // all bad values specified
    } // end try
    catch (invalid_argument& e)
    {
        cerr << "\n\nException while initializing t5: " << e.what() << endl;
    } // end catch*/
} // end main

