#include <stdio.h>
int main()
{
	int pack,i = 2,m = 60;
	printf("請輸入包裹的重量:\n");
	scanf_s("%d", &pack);
    while (i<pack && pack>2)
	{
		i += 1, m += 35;
	}
	printf("所需運費:%d",m);
  return 0;
}