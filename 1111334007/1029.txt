#include<stdio.h>
float calculate(float height, float weight);

int main()
{
	float height, weight, bmi;
	printf("請輸入身高、體重 包含小數點後一位\n");
	scanf_s("%f %f", &height, &weight);
	printf("你的bmi指數為%.2f", calculate(height, weight));
}
float calculate(float height, float weight)
{
	return weight / (height * height);
}