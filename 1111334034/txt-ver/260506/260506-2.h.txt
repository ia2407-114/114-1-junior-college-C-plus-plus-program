#include <iostream>
#include <cmath>
using namespace std ;
class Matrix
 {         
  public: 
    int row,column;
    double **element ; 
	
    Matrix(int m, int n) ;
        
    // 定義矩陣的"-"(取負)前置運算子多載函數
    Matrix operator-( ) ;

    Matrix operator!( ) ;

    // 定義矩陣的後序"++"運算子多載函數
    Matrix operator++(int) ;
 } ;