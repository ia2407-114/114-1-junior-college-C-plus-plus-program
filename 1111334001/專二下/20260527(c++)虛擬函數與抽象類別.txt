主程式(main.cpp)
#include <iostream>
#include"Volume.h"
using namespace std;

int main() {
    Shape* a1;
    Cone c(6,7);
    Ball b(5);
    Cylinder cy(7, 2);

    a1 = &c;
    cout << "圓錐體體積為"<<a1->Volume() << endl;
    a1 = &b;
    cout << "球體體積為" << a1->Volume() << endl;
    a1 = &cy;
    cout << "圓柱體體積為" << a1->Volume() << endl;

    return 0;
}
--------------------------------------------------------------------------
資源檔(Volume.cpp)
#include <iostream>
#include"Volume.h"
using namespace std;


    double Cone::Volume() const  {
        return 3.14159 * radius * radius * height * (1.0 / 3.0);
    }




    double Ball::Volume() const {
        return 3.14159 * radius * radius * radius * (4 / 3);
    }




    double Cylinder::Volume() const  {
        return 3.14159 * radius * radius * height;
    }
--------------------------------------------------------------------------
標頭檔(Volume.h)
#include <iostream>
using namespace std;

class Shape {
public:
    virtual double Volume() const = 0;
};

class Cone : public Shape {
private:
    double radius;
    double height;

public:
    Cone(double r, double h) : radius(r), height(h) {}

    double Volume() const override;
};


class Ball : public Shape {
private:
    double radius;

public:
    Ball(double r) : radius(r) {}

    double Volume() const override;
};

class Cylinder : public Shape {
private:
    double radius;
    double height;

public:
    Cylinder(double r, double h) : radius(r), height(h) {}

    double Volume() const override;
};